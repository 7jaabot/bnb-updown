## Session — 14:30

### Done
- Fixed critical bug: live multi-combined trades executing on-chain but not recorded in dashboard (0W/0L). Root cause: `bnb_price` variable out of scope between Phase 1 (prefetch, returns early) and Phase 2 (sniper shot, separate `_tick()` call) — `UnboundLocalError` silently caught.
- Removed hardcoded `600.0` BNB price fallback: Phase 1 now skips TX pre-sign if no WS price; Phase 2 uses `0.0` as "no data" marker for metadata.
- Ran `recover_claims.py` — 13 epochs checked, nothing claimable (all already claimed or losses).
- Updated `/review` skill to flag hardcoded fallback values as a code quality check.

### Pending
- Bot needs restart to pick up the fix. Next trades should appear in dashboard.

### Notes
- The coordination state (`logs/live/.coordination/`) correctly tracked all 13 on-chain trades — useful recovery source if trade logs diverge again.

## Session — 17:15

### Done
- Discovered position sizing was inconsistent: CLAUDE.md said "Flat $10", config had `position_size_usdc: 10`, but live code did `5% of wallet` (hardcoded in main.py). Paper used flat $10.
- Full refactor: unified to `bankroll_pct: 0.02` (2% du bankroll, min $5, max 10% pool) partout — config, compute_position_size(), 17 stratégies, main.py, tests, CLAUDE.md.
- Fixed `min_position_usdc` config (was 10.0, now 5.0 to match the rule).
- Reinforced `/review` skill section 1.7 with concrete config↔code↔doc coherence checks.

### Pending
- None.

### Notes
- compute_position_size() now takes (bankroll, bankroll_pct, min_usdc, max_pool_pct, pool_total_usdc) and returns a float. Backward-compat args (kelly, edge, p_up, etc.) removed.
- Live mode: bankroll synced from on-chain wallet each epoch before evaluate(). Paper: tracked via PnL after each trade.
