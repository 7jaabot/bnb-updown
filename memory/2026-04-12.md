## Session — 14:30

### Done
- Fixed critical bug: live multi-combined trades executing on-chain but not recorded in dashboard (0W/0L). Root cause: `bnb_price` variable out of scope between Phase 1 (prefetch, returns early) and Phase 2 (sniper shot, separate `_tick()` call) — `UnboundLocalError` silently caught.
- Removed hardcoded `600.0` BNB price fallback: Phase 1 now skips TX pre-sign if no WS price; Phase 2 uses `0.0` as "no data" marker for metadata.
- Ran `recover_claims.py` — 13 epochs checked, nothing claimable (all already claimed or losses).
- Updated `/review` skill to flag hardcoded fallback values as a code quality check.

### Pending
- Bot needs restart to pick up the fix. Next trades should appear in dashboard.

### Notes
- The coordination state (`logs/live/.coordination/`) correctly tracked all 13 on-chain trades — useful recovery source if trade logs diverge again.
